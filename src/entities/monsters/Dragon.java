package entities.monsters;

public class Dragon extends Monster {
    public Dragon(String description, int currentHealthPoints, int probability, int damage) {
        super(description, currentHealthPoints, probability, damage);
    }
}
